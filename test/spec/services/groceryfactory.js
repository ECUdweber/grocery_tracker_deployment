'use strict';

describe('Service: groceryFactory', function () {

  // load the service's module
  beforeEach(module('angularGroceryTrackerApp'));

  // instantiate service
  var groceryFactory;
  beforeEach(inject(function (_groceryFactory_) {
    groceryFactory = _groceryFactory_;
  }));

/*
  it('should do something', function () {
    expect(!!groceryFactory).toBe(true);
  });
*/  

});
